# Rename this file to .env.local OR copy:  copy .env.example .env.local
#
# 1. Open https://platform.openai.com/api-keys
# 2. Create a secret key and paste it below (one line, no quotes, no spaces around =)

OPENAI_API_KEY=

# Optional (default is gpt-4o-mini):
# OPENAI_MODEL=gpt-4o-mini
